stream
        |from()
                .measurement('usage_idle')
                .where(lambda: "cpu" == 'cpu-total')
                
        |alert()
                .info(lambda: TRUE)
                .warn(lambda: "value < 80")
                .crit(lambda: "value < 70")
                .log('/tmp/alert.log')
